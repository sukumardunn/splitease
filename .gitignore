
# macOS Finder metadata
.DS_Store

# Agent worktrees (transient, each is its own checkout)
.claude/worktrees/

# Screenshot harness deps (scripts/screenshots/npm install)
node_modules/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
