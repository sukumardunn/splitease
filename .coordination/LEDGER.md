# Coordination Ledger

**Live claim board.** Read [`PROTOCOL.md`](PROTOCOL.md) first. Add a row BEFORE
you edit any file, commit the claim before code, heartbeat every ~15 min, set
`DONE` when finished. Stale claims (heartbeat > 30 min) may be reclaimed.

**Base branch:** cut agent branches from **`claude-driven-changes`** and commit
ledger updates there. **`master` is frozen** — no commits of any kind
(PROTOCOL §0).

Timestamps are ISO-8601 UTC. Keep this table sorted by `claimed_at`.

| agent  | status | branch | scope | task | claimed_at | last_heartbeat | notes |
|--------|--------|--------|-------|------|------------|----------------|-------|
| agent1 | DONE | agent/phase1-persistence-agent1 | `jsapps/src/services/**`, `jsapps/src/context/AppContext.tsx`, `jsapps/src/types.ts`, `jsapps/src/components/expenses/AddExpenseModal.tsx`, `jsapps/src/App.tsx`, `jsapps/src/pages/Dashboard.tsx`, `jsapps/src/pages/Activity.tsx`, `jsapps/src/utils/helpers.tsx`, `jsapps/**/*.test.ts*`, `jsapps/vitest.config.ts`, `jsapps/package.json` | Phase 1: on-device persistence + calculator + payer fix + tests | 2026-07-18T14:04:43Z | 2026-07-18T14:12:00Z | DONE. Phase 1 shipped: localStorage persistence, tested split/balance calc, paid-by selector, typecheck-gated build, 16 tests. Scope free for other agents. |
| agent2 | DONE | agent/phase2-3-agent2 | `jsapps/src/services/**`, `jsapps/src/context/AppContext.tsx`, `jsapps/src/types.ts`, `jsapps/src/components/expenses/**`, `jsapps/src/pages/Activity.tsx`, `jsapps/src/pages/Expenses.tsx`, `jsapps/src/pages/Friends.tsx`, `jsapps/src/pages/RecentlyDeleted.tsx`, `jsapps/src/components/ui/**`, `jsapps/src/App.tsx`, `jsapps/src/components/layout/**`, `jsapps/**/*.test.ts*` | Phase 2 (soft-delete + undo + real activity/audit log) & Phase 3 (Splitwise-parity split engine, multi-payer, first-class settle-up, debt simplification) | 2026-07-18T14:36:38Z | 2026-07-18T15:10:47Z | DONE. Phases 2 & 3 shipped on branch agent/phase2-3-agent2 (commits 0cc44d5→7cd4294, built on Phase 1). 65 tests, typecheck+build clean. Scope free. NOTE for integrator: master still lacks Phase 1 — merge order is phase1 → phase2-3. Phases 4-7 (Supabase backend/auth/CSV import/attachments/hosting) UNCLAIMED — they need a Supabase project + credentials + deploy decisions (user input required), so left for a future session. ENV quirk: run vitest/vite under Node 20 (`nvm use 20`); local default Node 16 crashes them. |
| agent3 | DONE | agent/phase4a-orchestrator-agent3 | (see below) | Phase 4a orchestrator (re-ACTIVE, reclaimed back from stale agent4 at 2026-07-19T04:36:24Z) | 2026-07-18T20:52:04Z | 2026-07-19T05:42:56Z | STALE/reclaimed by agent4 2026-07-19T00:25Z. agent3 committed T4+T5 code to their branches but crashed before merging/board-update. agent4 took over: verify+merge T4/T5, then T6/T7/T8. |
| agent4 | DONE | feature/phase4a (+ agent/4a-t*-agent4 task branches) | `.coordination/PHASE4A_BOARD.md`, `jsapps/src/services/supabaseStore*`, `jsapps/src/context/**`, `jsapps/src/components/auth/**`, `jsapps/src/components/import/**`, `jsapps/src/components/layout/Sidebar.tsx`, `jsapps/src/App.tsx`, `supabase/**`, `jsapps/.env.local` | Phase 4a: reclaim+merge T4 (supabaseStore) & T5 (auth); execute T6 (AppContext optimistic refactor), T7 (localStorage import), T8 (stack bring-up). Board is authoritative for per-task sub-claims. | 2026-07-19T00:25:00Z | 2026-07-19T00:25:00Z | Took over from stale agent3. Orchestrating Sonnet subagents per docs/superpowers/plans/2026-07-19-phase4a-auth-persistence.md. |
| agent5 | DONE | feature/phase4a→master | `jsapps/.env.local`, `jsapps/src/lib/database.types.ts` (reconcile only), `supabase/config.toml`, `.coordination/PHASE4A_BOARD.md` | Phase 4a T8: live verification via hosted Supabase (Docker-free); apply migration, §9 acceptance, reconcile types, merge feature/phase4a→master. Plan: `~/.claude/plans/composed-zooming-alpaca.md`. | 2026-07-24T17:32:38Z | 2026-07-24T18:33:34Z | DONE. T8 complete + Phase 4a merged to master (`8a0451c`). Migration live (9 tables/RLS/trigger); gate green (Node26 build/lint/typecheck, Node20 tests 85/85); backend acceptance 13/13; types 0-drift; browser smoke passed. Scope free. Pre-existing non-4a cosmetic bug logged: Sidebar Overall Balance hardcoded $355. |
| agent7 | DONE | agent/add-friend-agent7 | `jsapps/src/services/supabaseStore.ts`, `jsapps/src/context/AppContext.tsx`, `jsapps/src/pages/Friends.tsx`, `jsapps/src/components/friends/**`, related `*.test.ts*` | P0 (pre-Phase-5): implement the Add Friend flow — the buttons in Friends.tsx have no handler and no `addFriend`/`insertFriend` exists, so a new user cannot add anyone to split with. See `docs/ROADMAP.md`. | 2026-07-25T01:10:19Z | 2026-07-25T01:23:05Z | DONE. Merged to `claude-driven-changes` (`439fe98`). 200 tests; gate green on Node 20 AND 26. Verified live in-browser end-to-end on a fresh signup (add friend → split $50 → $25 owed → persisted; duplicate guard + Escape confirmed in the real UI; 0 console errors); smoke user deleted afterwards. Also fixed broken `<img>` avatars via a mapper-level generated-initials fallback, and extracted `useFocusTrap` (shared with ImportPrompt, whose 9 tests passed unchanged). Scope free. Next up per `docs/ROADMAP.md`: Phase 5 CSV import — needs `supabase/migrations/**` held alone for the `import_batch` table. Found during 4b browser smoke. Also gives `friend.add` (an ActivityAction with no producer) its first emitter. No migration needed — `friends` table + RLS already exist from 4a. |
| agent7 | DONE | agent/phase4b-agent7 | `jsapps/package.json`, `jsapps/vitest.config.ts`, `jsapps/src/test/**`, `jsapps/src/services/supabaseStore.ts`, `jsapps/src/context/AuthContext.tsx`, `jsapps/src/context/AppContext.tsx`, `jsapps/src/components/auth/**`, `jsapps/src/components/import/ImportPrompt.tsx`, `jsapps/src/components/layout/Sidebar.tsx`, `supabase/migrations/20260724*`, `docs/PHASE4B_BACKLOG.md`, `README.md` | Phase 4b: backlog items 1,2,3,4,6,7,8,9,10,11,12 (write-verification, union validation, auth hardening, activity_events RLS, a11y, sidebar balance, auth tests, jsdom/Node26) | 2026-07-24T20:29:10Z | 2026-07-24T20:54:00Z | DONE. Items 1,2,3,4,6,7,8,9,10,11,12 shipped; merged to `claude-driven-changes` (`855b282`). 85→170 tests; gate green on Node 20 AND 26 (test/build/typecheck/lint, 0 errors). Scope free. **⚠️ HANDOFF: `supabase/migrations/20260724000001_phase4b_activity_events_append_only.sql` is NOT yet applied to the hosted DB** — no supabase CLI/psql on this machine and the service-role key can't run DDL over REST; needs a dashboard SQL-editor paste. Item 5 (whole-state rollback granularity) intentionally NOT done — spec'd 4a tradeoff, needs a concurrency-model design decision. Item 12's original diagnosis was wrong: not a jsdom crash, no dep bump — Node ≥22's inert `localStorage` global made vitest skip jsdom's; fixed in `src/test/setup.ts`. |
| agent6 | DONE | claude-driven-changes | `README.md`, `CLAUDE.md`, `.coordination/**`, `.superpowers/sdd/progress.md`, `docs/superpowers/plans/**` | Docs: add README (setup + `.env.local`-not-in-git note), log Sidebar $355 bug + consolidate 4b deferred list, make `claude-driven-changes` the working line and freeze `master` | 2026-07-24T19:23:54Z | 2026-07-24T19:23:54Z | DONE. Docs-only, no app code touched. Local `master` reset to upstream `a8266c6` (verified beforehand: `git diff master claude-driven-changes` empty, no commits unique to master, `fb16279` pushed to `origin/claude-driven-changes`). New rule: all work on `claude-driven-changes`; `master` frozen. Scope free. |
| agent8 | DONE | agent/phase5a-csv-import-agent8 | `supabase/migrations/**` (held ALONE), `jsapps/src/lib/database.types.ts`, `jsapps/src/services/csvImport*.ts`, `jsapps/src/services/supabaseStore.ts`, `jsapps/src/context/AppContext.tsx`, `jsapps/src/components/import/**`, `jsapps/src/pages/Expenses.tsx`, `jsapps/src/types.ts`, related `*.test.ts*`, `docs/ROADMAP.md` | P1 Phase 5A: CSV import — upload Splitwise CSV → column mapping → dry-run preview with duplicates flagged → confirm → `import_batch` so the whole import can be undone. See `docs/ROADMAP.md` and design §5.2. 5B (screenshot/OCR) explicitly OUT of scope. [2026-07-25: 5B was CANCELLED outright by the user — never build it.] | 2026-07-25T02:57:15Z | 2026-07-25T03:45:00Z | DONE. Merged to `claude-driven-changes` (`6e6729a`). 262→281 tests; gate green on Node 20 AND 26 (test/typecheck/build/lint, 0 errors). **`supabase/migrations/**` is FREE again — migration `20260725000001` is APPLIED to the hosted project and verified** (table, both nullable `import_batch_id` columns, 3 indexes, RLS + owner policy, both FKs `on delete set null`). Applied via `SUPABASE_DB_URL` as the `postgres` superuser with a throwaway `pg` script outside the repo, wrapped in a transaction — see the ROADMAP env-gotchas note; no CLI/psql needed and no dep added to the app. Verified live in-browser end-to-end on a fresh signup: 6-row export → 4 expenses + 2 friends, every payer reconstructed from the signed net columns, balances hit the hand-computed $77 and survived a hard reload; re-import flagged all 4 as duplicates and disabled confirm; undo removed the 4 expenses and Bob but KEPT Alice (referenced by a hand-entered expense by then), 0 orphaned split/payer rows, 0 console errors. Smoke user deleted afterwards. Scope also touched `activityLog.ts`/`dbValidation.ts` (new `import.create`/`import.undo` actions + `import` entity type) and `src/test/setup.ts` (jsdom 25 has no `Blob.prototype.text`). Scope free. ~~Next per `docs/ROADMAP.md`: P1 Phase 5B (screenshot/OCR)~~ **CANCELLED 2026-07-25 by user decision — do not build OCR import, and do not re-propose it.** CSV is the only import path; see `docs/ROADMAP.md`. Found while verifying: 3 leftover test `auth.users` from earlier phases, logged under P4. |
| agent9 | DONE | agent/phase6a-notes-analytics-agent9 | `jsapps/src/pages/Analytics.tsx` (new), `jsapps/src/components/analytics/**` (new), `jsapps/src/services/analytics.ts` (new), `jsapps/src/components/expenses/AddExpenseModal.tsx`, `jsapps/src/components/expenses/ExpenseItem.tsx`, `jsapps/src/App.tsx`, `jsapps/src/components/layout/Sidebar.tsx`, `jsapps/src/components/layout/MobileMenu.tsx`, related `*.test.ts*`, `docs/ROADMAP.md` | P2 Phase 6A: wire the unused `notes` field into the expense UI, and add an Analytics page (category breakdown, monthly trend, person-wise net) with hand-rolled inline SVG. Receipt attachments (Storage + `receipt_url` migration) explicitly OUT of scope — user chose notes+analytics only. | 2026-07-25T04:24:55Z | 2026-07-25T07:12:45Z | DONE. Merged to `claude-driven-changes` (`c10b11d`). 281→327 tests; gate green on Node 20 AND 26 (test/typecheck/build/lint, 0 errors, same 3 pre-existing `react-refresh` warnings). **No migration and no new dependency** — `notes` already round-tripped through both mappers, and the charts are hand-rolled HTML/Tailwind (not SVG as the claim guessed), so the 5-runtime-dep budget is untouched. `supabase/migrations/**` was never claimed and is still free. Verified live in-browser on a fresh signup against the hosted DB: 2 friends + 5 expenses, every figure matched a hand computation ($620 spend / 5 expenses / $124 avg / largest Rent $500; categories Rent 500, Groceries 50, Transportation 50, Dining 20 with the 50/50 tie broken alphabetically; Bob +$520 teal-right, Alice −$10 red-left) and survived a full page reload; table view, hover tooltip, 5-col mobile nav and note truncation all confirmed; 0 console errors (only the 2 known React Router future-flag warns). **Palette was validated, not eyeballed** — the app's own green-600/red-600 balance convention FAILS CVD separation (deutan ΔE 5.0, below even the 6.0 floor), so the chart poles are teal-600/red-600 (ΔE 13.1) with bar direction + a printed sign as secondary channels; provenance and both rejected candidates are recorded in `components/analytics/chartTokens.ts`. Also fixed a pre-existing a11y bug found en route: the Description/Amount/Category labels in `AddExpenseModal` had no `htmlFor`/`id`, so they were not programmatically associated with their inputs. Scope free. **Next per `docs/ROADMAP.md`: P2 Phase 6B (receipt attachments) — that one DOES need `supabase/migrations/**` held alone plus a Storage bucket + policies**, unlike 6A. Also logged there: no UI can edit a note after creation (`updateExpense` still has zero UI callers), category percentages can sum to 101%, and a 4th leftover smoke account (`smoke6a_1784952000@`) deliberately NOT deleted — the sandbox blocked the throwaway script and working around a denial to run `DELETE` on a live auth table was the wrong call. |
| agent10 | DONE | agent/edit-expense-agent10 | `jsapps/src/components/expenses/AddExpenseModal.tsx`, `jsapps/src/components/expenses/ExpenseItem.tsx`, `jsapps/src/components/analytics/CategoryBars.tsx`, `jsapps/src/services/analytics.ts`, related `*.test.ts*`, `docs/ROADMAP.md` | 6A follow-ups: give `updateExpense` its first UI caller (edit-expense surface — it has existed since 4a with zero callers, so nothing in the app can edit an expense after creation, including 6A's notes), and largest-remainder the category percentages that can sum to 101%. NO migration, NO new dependency, NO Storage/infra — `supabase/migrations/**` deliberately NOT claimed and stays free for 6B. | 2026-07-25T08:30:27Z | 2026-07-25T08:57:10Z | DONE. Merged to `claude-driven-changes` (`bed89e0`). 327→370 tests; gate green on Node 20 AND 26 (test/typecheck/build/lint, 0 errors, **still the same 3 pre-existing `react-refresh` warnings** — an export of `deriveFormSeed` from the modal briefly made it 4, so the seeding logic lives in `components/expenses/expenseFormSeed.ts`). **No migration, no new dependency, no Storage** — `supabase/migrations/**` was never claimed and is still FREE for 6B. Verified live in-browser against the hosted DB on a fresh signup: multi-payer (70/20) + uneven exact split (50/40) round-tripped through a hard reload and re-seeded correctly, note cleared to genuine SQL NULL, `date` and `import_batch_id` preserved across 5 consecutive edits, and a DB query confirmed exactly 2 payer + 2 split rows with **0 orphaned child rows project-wide** — the store's delete-then-reinsert child replace had never once run before this UI existed. Smoke account deleted afterwards (cascade verified, 0 leftovers). Also: `paidBy` no longer derives from `payerIds[0]` (that order comes from an unordered select and shifted when a payer was unchecked/re-checked, silently reassigning who paid); a lone `expense_payers` row is preserved rather than deleted (a 5A import always writes exactly one); payer/split amount inputs got `aria-label`s (DevTools flagged 7 unlabeled fields). Scope free. **⚠️ HANDOFF — logged under P4, read before touching expense writes: `supabaseStore.updateExpense` replaces child rows across 4 non-transactional requests, so if the deletes land and the re-insert fails, the expense is left with ZERO splits while the toast says "it was undone" — which is false. Pre-existing since 4a, but this UI is the first thing that can trigger it.** Two known, now-pinned-by-test limitations: split mode is inferred not stored (a percentage/shares expense reopens as `exact`), and editing an expense the current user isn't on adds them at $0. |
| agent10-w2 | DONE | agent/w2-* (five worktree branches, see notes) | **A:** `supabase/migrations/**` (held ALONE), `jsapps/src/services/supabaseStore.ts`, `jsapps/src/lib/database.types.ts` · **B:** `jsapps/src/hooks/useFocusTrap.ts` · **C:** `jsapps/src/components/expenses/ExpenseItem.tsx`, `jsapps/src/utils/categoryColors.ts` (new), `jsapps/src/components/analytics/chartTokens.ts` · **D:** `jsapps/src/pages/Analytics.tsx`, `jsapps/src/components/analytics/CategoryBars.tsx`, `jsapps/src/services/analytics.ts` · **E:** read-only + `docs/RLS_AUDIT.md` (new) · plus each one's own `*.test.ts*`. `docs/ROADMAP.md` and `.coordination/**` reserved to the coordinator. | Wave 2 — the roadmap items that need no user decision, run as five parallel agents in isolated git worktrees: (A) make `updateExpense`'s child replace atomic, the P4 data-loss hazard logged today; (B) restore focus to the trigger in `useFocusTrap`; (C) fix the dynamic-Tailwind category colours that emit no CSS; (D) make Analytics derive one aggregate instead of two that can silently disagree; (E) audit RLS across every table (4b tightened only `activity_events`) and report — no policy changes applied. | 2026-07-25T09:13:25Z | 2026-07-25T09:31:40Z | DONE. Six agents merged into `claude-driven-changes` (`a79e992`…`4455fa8`); 370→414 tests, gate green on Node 20 AND 26 (test/typecheck/build/lint, 0 errors, same 3 `react-refresh` warnings). Migrations `20260725000002/0003/0004` authored AND applied to the hosted project, each verified live with real user JWTs (15/15 checks incl. cross-tenant rejection and forced-mid-body-failure atomicity). A 6th agent was added mid-wave after agent A found the same non-atomic bug in `insertExpense` and `updateGroup`; the coordinator fixed the third instance (`insertGroup`) directly. `supabase/migrations/**` is FREE again. **⚠️ `npx tsc --noEmit` typechecks NOTHING here — use `npm run typecheck`;** see the env-gotchas section. Coordinated by one session, so worktree isolation replaces cross-session claim negotiation; scopes are disjoint by construction. E is deliberately report-only so it cannot contend with A for `supabase/migrations/**`. |
| agent11 | DONE | agent/w3-* (worktree branches, see notes) | **A:** `supabase/migrations/**` (held ALONE), `jsapps/src/lib/database.types.ts`, `jsapps/src/services/supabaseStore.ts` · **B:** `jsapps/src/components/expenses/**`, `jsapps/src/types.ts`, `jsapps/src/context/AppContext.tsx` · **C:** docs-only (`docs/**`, `README.md`, `.superpowers/**`, `jsapps/.bolt/prompt`) · **D:** `jsapps/supabase/**` (delete), root `supabase/config.toml` · plus each one's own `*.test.ts*`. `.coordination/**` reserved to the coordinator. | Wave 3 — the user's eight decisions from 2026-07-25: drop OCR/5B entirely from the docs (never building it), receipts stored **in Postgres, not a Storage bucket**, persist the split mode (same expenses migration), delete the dead `jsapps/supabase/` + add a root `config.toml`, make `importState` atomic, leave the React Router warnings, defer the key rotation to v1.0. Rollback granularity awaits the user's choice of concurrency model. | 2026-07-25T09:45:00Z | 2026-07-25T15:50:00Z | DONE. All eight decisions handled; merged to `claude-driven-changes` (`5f95684`…`6f6f3a2`). 414 → **507 tests**, gate green on Node 20 AND 26 (test/typecheck/build/lint, 0 errors, same 3 pre-existing `react-refresh` warnings). Migrations `20260725000005/0006/0007` authored, applied to the hosted project AND verified live with real user JWTs (33/33 receipts+split_mode, both forced-failure + 4 cross-tenant cases for the atomic import, 13/13 for the split_mode follow-up). `supabase/migrations/**` is FREE again. **`jsapps/supabase/` is DELETED** and `supabase/config.toml` now pins the migrations path at the repo root (`major_version = 17`, matching the hosted `server_version 17.6`) — running `supabase db reset` from `jsapps/` can no longer apply the wrong schema. **Phase 5B (screenshot/OCR) is CANCELLED — do not build it, do not re-propose it.** Receipts deliberately do NOT use a Storage bucket; bytes live in `expense_receipts`, fetched only on open. Scoped rollback replaced the whole-state snapshot; **keep the narrowing inside `mutate()`** so a future mutation queue stays a one-function swap. Still open and logged in `docs/ROADMAP.md`: same-row last-write-wins, a truly concurrent two-tab double-import, and the `remoteEmpty` guard ignoring `activityEvents`. `SUPABASE_SECRET_KEY` rotation is deferred to v1.0 by the user's choice — raise it then, not before. Hosted project left clean: 0 `auth.users`, 0 rows. Scope free. |
| agent12 | DONE | agent/screenshot-harness-agent12 | `scripts/screenshots/**` (new), `.gitignore`, `working_prototype_screenshots/**` | Add a re-runnable UI screenshot harness (Playwright) so each phase can be captured as `working_prototype_screenshots/v<N>` and compared. Tooling only — no `jsapps/` app code, no migrations, no new app dependency. | 2026-07-25T17:01:26Z | 2026-07-25T17:22:00Z | DONE. Merged to `claude-driven-changes` (`078524b`). Scope free. **⚠️ THE HOSTED PROJECT IS NO LONGER A CLEAN SLATE — this reverses the "0 users, 0 rows" state agent11 left.** `demo@splitease.local` (pw `demo1234`) now holds 5 friends, 4 groups, 31 expenses, 97 splits, 1 settlement, 35 activity events, 2 soft-deleted expenses + 1 deleted group, 1 receipt. **The user wants this kept for the next phase — do NOT delete it.** `node seed.js` is non-destructive; only `--reset` rebuilds. Harness drives installed Chrome so `jsapps/` gains no dependency; verified by regenerating v0 from the committed script (21/21, 0 console errors). Two known warnings it emits: `modal-settle-up` and `modal-new-group` produce no `[role="dialog"]`, so those two shots may show the page behind them — worth a look when someone next touches those surfaces. No dark mode exists to capture (no `dark:` classes, no `darkMode` in tailwind config), so v0 is light-only. |

<!--
Copy this row template, fill it in, delete the example above once real claims exist:

| agentN | ACTIVE | agent/<task>-agentN | `src/pages/Foo.tsx`, `src/components/foo/**` | build Foo screen | 2026-07-18T14:00:00Z | 2026-07-18T14:00:00Z |  |
-->
